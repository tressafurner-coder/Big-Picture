---
description: Keep prototype “last update” strings in sync with shipped changes
globs: "**/*.{ts,tsx}"
alwaysApply: true
---

# Last update stamps

The **prototypes hub** table reads each row’s date from that prototype’s source.

## Chatbot prototype (`ChatbotPrototype`, chat components)

Update **`src/app/lastUpdate.ts`**: set `CHATBOT_LAST_UPDATE_DISPLAY` when those change (e.g. `date '+%d %b %Y, %H:%M'`).

## Independent prototype (`prototypes/independent/`)

Update **`prototypes/independent/src/prototypeLastEdited.ts`**: set `INDEPENDENT_LAST_EDITED` when that app changes.

## Teams dropdown prototype (`TeamsDemoPage`, `BigPictureTeamsDropdown`)

Update **`src/app/teamsPrototypeMeta.ts`**: set `TEAMS_DEMO_LAST_EDITED` when that flow changes.

Hub-only copy/layout changes on `PrototypesHub` do not require bumping other prototypes’ dates unless you change those prototypes too.
